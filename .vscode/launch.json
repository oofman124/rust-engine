{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Game",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/game",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build",
      "sourceLanguages": ["rust"]
    }
  ]
}
